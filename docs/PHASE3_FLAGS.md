# Phase 3 Feature Flag Ledger

Tracked per `docs/PHASE3_EXECUTION_PLAN.md` §2 risk "Feature-flag sprawl." Every flag introduced during Phase 3 is listed here from the commit that adds it until the commit that resolves it (removed + old path deleted, per 3.6). None may reach Phase 3.6 unresolved.

| Flag | Default | States | Introduced | Status |
|---|---|---|---|---|
| `DATABASE_BACKEND` | `sqlite` | `sqlite`, `postgres` (reserved, unused) | 3.1 (`4da4913`) | Open — global seam, not yet consumed by any domain; superseded in practice by per-domain flags below. |
| `PROJECTS_BACKEND` | `sqlite` | `sqlite`, `dual` | 3.2 (`d235c86`) | Open — code written; initial backfill run + parity check against real Supabase completed 2026-07-12, 0 drift (3 migrated, 1 already-linked). Extended Staging soak not yet started; `dual` not yet enabled in any deployed environment. A `postgres` (read-cutover) state is out of scope for 3.2 — see plan §3.2 note. |
| `KB_BACKEND` | `sqlite` | `sqlite`, `dual` | 3.3 (`57e62fa`) | Open — code written; initial backfill run + parity check against real Supabase completed 2026-07-12, 0 drift (0 KB documents exist locally, nothing to migrate). Extended Staging soak not yet started. Covers create+delete(as archive) only — extracted-text sync is explicitly NOT dual-written (see config.py comment); backfill does a one-time copy only. |
| `EQUIPMENT_BACKEND` | `sqlite` | `sqlite`, `dual` | 3.4 (`a796f8f`) | Open — code written; initial backfill run + parity check against real Supabase completed 2026-07-12, 0 drift (0 equipment records exist locally, nothing to migrate). Extended Staging soak not yet started. Covers create/update/(real)delete + kb-sourced link/unlink only. |
| `QMS_BACKEND` | `sqlite` | `sqlite`, `dual` | 3.5 (`7dc015e`) | Open — code written; initial backfill run + parity check against real Supabase completed 2026-07-12, 0 drift (7 records migrated across all 4 types). Extended Staging soak not yet started. One flag for all four record types (deviations/capas/change_controls/risk_assessments). Covers title/status/project_id + audit_trail only — sub-structures (investigation/impact/actions/effectiveness/etc.) have no target-schema table and are out of scope entirely; attachments/comments/approvals dual-write deferred to a follow-up. |

**2026-07-12 validation run:** discovered and fixed two real bugs blocking backfill: (1) migrations 0005–0008 added RLS policies for every Phase 3.2–3.5 table but never the paired GRANT statements — same class of bug 0003 already fixed once for the identity tables — fixed via `migrations/0009_phase3_grants_up.sql` (applied manually in Supabase SQL Editor, no automated migration runner exists in this repo); (2) the local `pharmagpt/pharmagpt.db` file predated the `postgres_id` bookkeeping columns being added in code — `init_db()` (idempotent, additive-only) had not been re-run since — fixed by running it once. One project row from a crashed first attempt (pre-fix) was retroactively linked to its SQLite source rather than left as a duplicate-risk orphan. Full pytest suite: 305 passed, 0 failed after all of the above.

**Still required before Phase 3.6 (per plan §3, unchanged/absolute):** an actual extended Staging soak of each flag running in `dual` for a real period (this session performed a one-shot backfill + single parity check only, not a soak), and the 2-company RLS isolation spot-check. Neither has been performed yet.

**Resolution owner:** whoever flips a flag in Staging/Production is responsible for updating this table's Status column in the same PR/commit.
