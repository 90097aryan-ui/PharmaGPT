# Phase F — Finding Traceability Matrix

**Baseline established:** 2026-07-24, at the start of Phase F, before any Phase F code changes.
**Source documents traced:** `ENTERPRISE_ACCEPTANCE_TEST_REPORT.md` (2026-07-23, prior formal acceptance test, verdict NO-GO), `PHARMAGPT_v1.0_RELEASE_READINESS_REPORT.md` (2026-07-24, Phase E independent audit, verdict NO-GO, 52%).
**Rule applied throughout:** a finding is marked **Fixed** only when this document (or a linked Phase F report) cites source code, an automated test, and/or a runtime check that proves it. Anything short of that is **Not Verified** or **Open**, never assumed.

This matrix is a living document — it is updated in place as Phase F work packages land fixes. The column **Status (Phase F end)** is filled in at the close of Phase F, not pre-filled optimistically. Where a fix is applied later in this phase, the corresponding row is updated with the commit/file evidence at that time rather than duplicated into a second table.

---

## A. Findings carried from the prior formal acceptance test (`ENTERPRISE_ACCEPTANCE_TEST_REPORT.md`, 2026-07-23)

| # | Finding | Root Cause | Fixed? (at Phase F start) | Evidence | Verification | Status (Phase F end) |
|---|---|---|---|---|---|---|
| A1 | Company Administration (list/manage companies) fails end-to-end | Postgres migrations `0010_break_glass_rls_up.sql` / `0011_companies_admin_rls_up.sql` / `0012_users_company_admin_rls_up.sql`'s trailing `GRANT`/`CREATE POLICY` statements are not active in the live Supabase project | **No** | `ENTERPRISE_ACCEPTANCE_TEST_REPORT.md` §1 (Critical/FAIL); re-confirmed as unresolved by Phase E synthesis (no later document shows it re-tested to GO) | Not re-tested live in Phase E (credential-entry constraint, see Phase E report §0) | See WP2/WP4 sections below — **this specific finding requires a live Supabase SQL Editor action against the deployed database, which is outside what this environment can execute or verify (no live DB session/credentials available to this session).** Marked **NOT VERIFIED — operational action required, cannot be closed from source-code changes alone.** |
| A2 | User Management (list/update) fails for company_admin | Same root cause as A1 — dependent on the same grants | **No** | `ENTERPRISE_ACCEPTANCE_TEST_REPORT.md` §2 | Not re-tested live | **NOT VERIFIED — same operational dependency as A1.** RBAC/code-path itself reviewed and confirmed correct in WP4 (the code does the right thing *if* the grants are active); the failure is exclusively at the database-grant layer, not application logic. |
| A3 | Assume Company Context (break-glass) fails end-to-end | Same root cause as A1 | **No** | `ENTERPRISE_ACCEPTANCE_TEST_REPORT.md` §4 | Not re-tested live | **NOT VERIFIED — same operational dependency as A1.** `routes/auth.py`'s `assume_company`/`end_assume_company` logic independently re-read in Phase E and again in WP4 and found correct; the failure is exclusively at the database-grant layer. |
| A4 | Raw Postgres/Gemini exception text leaks to client on some error paths | Backend exceptions not translated before being returned | Partially — pre-existing generic error-page/JSON infrastructure exists (`app.py` errorhandlers) but not applied at every raw-exception call site | `ENTERPRISE_ACCEPTANCE_TEST_REPORT.md` §7.2 | Not in Phase F work-package scope (not a Critical/High compliance blocker; explicitly out of scope per Phase F's "only fix verified release blockers") | **Open, out of scope for Phase F** — carried forward as a residual risk, not addressed. |
| A5 | `routes/users.py` 403-wording inconsistency | Minor message-text inconsistency, not a logic bug | N/A — cosmetic | `ENTERPRISE_ACCEPTANCE_TEST_REPORT.md` §7.1 | Not in Phase F scope | **Open, out of scope for Phase F.** |
| A6 | No audit-log viewer for break-glass grants | Product/UI gap, not a logic bug | N/A — feature gap | `ENTERPRISE_ACCEPTANCE_TEST_REPORT.md` §5 | Out of scope — Phase F explicitly forbids new features | **Open, explicitly out of scope (new-feature prohibition).** |

---

## B. Findings from the Phase E independent audit (`PHARMAGPT_v1.0_RELEASE_READINESS_REPORT.md`, 2026-07-24) — Critical

| # | Finding | Root Cause | Fixed? (at Phase F start) | Evidence | Verification | Status (Phase F end) |
|---|---|---|---|---|---|---|
| C1 | Company Administration / Assume Company Context non-functional | = A1/A2/A3 above | No | See A1-A3 | — | **NOT VERIFIED — operational, see A1.** |
| C2 | No server-side gate prevents OQ/PQ execution before IQ exists/completes | `create_protocol`/`execute_test_case` never check sibling-protocol status | No | Phase E report §10, `qual_database.py:425-453`, `routes/qual.py:412-440` | **FIXED, VERIFIED by automated test.** `routes/qual.py::execute_test_case` now blocks (409) executing an OQ/PQ test case unless the prior stage's protocol has a completed status. Gate scoped to execution only, not creation, after root-causing 2 test failures against the original (broader) implementation — see `docs/WORKFLOW_VALIDATION_REPORT.md` §1. Proven by `tests/test_phase_f_compliance.py::test_oq_test_execution_blocked_before_iq_complete` and `::test_oq_test_execution_allowed_once_iq_complete`, both passing. |
| C3 | Validation Report can be approved/released without linked Qualification/PQ being complete | `report.py` create/generate/approval paths never query qualification completeness | No | Phase E report §10, `routes/report.py:85-114,397-438` | **FIXED, VERIFIED by automated test.** `routes/report.py::add_approval` now blocks (409) the "approved" transition when a linked qualification's PQ isn't complete. Linkage itself remains optional (see scope decision in `docs/WORKFLOW_VALIDATION_REPORT.md` §2) — this matches the literal finding ("no check that the *linked* qualification is complete") without mandating linkage, which would be a business-rule change beyond this fix's scope. Proven by 3 passing tests in `tests/test_phase_f_compliance.py`. |
| C4 | PUT/DELETE unaudited in every domain except `projects.py` | `add_audit_entry()` wired into create + approval only | No | Phase E report §7, per-domain grep table | **FIXED, VERIFIED by automated test.** `pharmagpt/audit.py` (new shared helper) is now called from every create/update/delete across Equipment, URS, Qualification/Protocols, Validation Report, QMS Document, Deviation, CAPA, Change Control, Risk Assessment, Knowledge Base, and Project Documents — see `docs/AUDIT_TRAIL_COVERAGE_REPORT.md` for the full per-domain table. Proven for Equipment by 3 passing tests asserting real rows in `qms_audit_trail`; the remaining domains follow the identical `audit.log()` call pattern, verified by direct code read. |
| C5 | Audit-trail schema has no old/new value or company_id columns | Schema designed around free-text `detail` field only | No | Phase E report §7, `qms_database.py:68-76` | **FIXED, VERIFIED.** `qms_audit_trail` extended with `company_id, old_values, new_values, reason, ip_address, session_id, result` (additive migration, `pharmagpt/database.py`). Schema change smoke-tested directly against the dev database; `old_values`/`new_values` diff-computation proven by a passing automated test (`test_equipment_update_writes_audit_entry_with_old_and_new_values`). |
| C6 | Identity spoofable in attachments/comments/protocol-completion/version-snapshot endpoints | `tenancy.signing_identity()` fix not propagated to these call sites | No | Phase E report §7 item 5 | **FIXED, VERIFIED for the comment endpoint by automated test** (`test_qms_comment_author_cannot_be_spoofed`, passing); attachment upload, protocol completion, and all four version-snapshot endpoints (URS/Qualification/QMS-Document/Validation-Report) fixed identically (`tenancy.signing_identity()`/server-derived identity replacing every client-supplied `performed_by`/`created_by`/`author`/`role` field) — verified by direct code read, not one dedicated test per site. |
| C7 | Several state-changing endpoints have no role guard (`risk.py:361` publish, `qual.py:443` complete, `qms_capa.py:220` escalate, `qms_documents.py` distribution/training/generate) | `@require_role` applied to canonical delete/approval endpoints only, not every alternate mutation path | No | Phase E report §7, code-audit agent findings | **FIXED for the 3 confirmed endpoints, VERIFIED by automated test** — `@require_role("company_admin","reviewer_qa")` added to Risk `/publish`, Qualification protocol `/complete`, and CAPA action `/escalate`; each now returns a real, asserted 403 for a `user`-role tenant and 200/201 for `reviewer_qa` (control case) in `tests/test_phase_f_compliance.py`. `qms_documents.py` distribution/training routes were reviewed and deliberately left unguarded — see `docs/RBAC_VERIFICATION_REPORT.md` §4 for the reasoning (routine record-keeping, not a QA sign-off; no evidence found that they should be restricted). AI-generation endpoints across all modules were confirmed to follow a consistent, deliberate "generation is open, approval is gated" pattern — not a gap. |

## C. Findings from the Phase E independent audit — High (in Phase F scope)

| # | Finding | Root Cause | Fixed? (at Phase F start) | Evidence | Status (Phase F end) |
|---|---|---|---|---|---|
| H7 | CAPA / Change Control have no `lifecycle_engine` guard at all — any status can follow any status | Never wired into the shared lifecycle engine, unlike Document/URS/Qualification/Report/Risk | No | Phase E report §6, `qms_capa.py`, `qms_change_control.py` | **PARTIALLY FIXED — the specific WP3 requirements are met; the broader gap is NOT VERIFIED as closed.** Terminal-state (`Closed`) immutability added to both modules' PUT and approval endpoints (matches WP3's "Archived records immutable" requirement) and proven for CAPA by a passing automated test. "Rejected doesn't bypass review" was already correct pre-existing behavior (re-verified: both map `Rejected` back to `Open`/`Draft`, not a skip). **Full arbitrary-transition prevention (any non-terminal status → any other non-terminal status) remains unbuilt** — deliberately not attempted, since a full transition graph for 13-status vocabularies would risk inventing unverified business rules, which the Phase F brief explicitly warns against. See `docs/WORKFLOW_VALIDATION_REPORT.md` §5 for the full reasoning. Recommended as scoped follow-up work, not claimed as done here. |

## D. Findings from the Phase E independent audit — High/Medium, explicitly OUT of Phase F scope

Per the Phase F brief ("DO NOT ADD NEW FEATURES. DO NOT REFACTOR FOR STYLE. DO NOT OPTIMIZE CODE. ONLY FIX VERIFIED RELEASE BLOCKERS.") and the work-package list (Audit Trail, Workflow Enforcement, RBAC, Company Isolation, Testing), the following Phase E findings are **not** addressed in Phase F and remain tracked as residual risk, to avoid scope creep beyond what was requested:

| # | Finding | Why out of scope | Disposition |
|---|---|---|---|
| H1 | DQ/FAT/SAT have no schema linkage to Equipment/Qualification | Traceability/data-model feature, not audit/workflow/RBAC/tenancy | Open, residual risk |
| H2 | DQ/FAT/SAT AI generation uses generic template, dedicated prompts dead code | Content-quality issue, not a compliance control | Open, residual risk |
| H3 | `risk_database.py` connection leak | Performance, explicitly excluded ("DO NOT OPTIMIZE CODE") | Open, residual risk |
| H4 | 6/7 AI-generation endpoints synchronous in-request | Performance, explicitly excluded | Open, residual risk |
| H5 | No caching of per-request auth/tenant resolution | Performance, explicitly excluded | Open, residual risk |
| H6 | SQLite→Postgres cutover soak/spot-check not performed | Operational/deployment action, not a code fix this session can perform | Open, residual risk |
| M2 | Lifecycle vocabularies missing distinct "Archived" state (QMS Document, URS) | Adding a new status value is closer to a schema/feature change than a "verified release blocker" fix; WP3's "Archived records immutable" requirement is honored below for whichever states currently exist, without inventing a new one | Open, residual risk — documented, not fixed |

---

*This matrix is updated in place, section by section, as each Phase F work package completes below. See `docs/AUDIT_TRAIL_COVERAGE_REPORT.md`, `docs/WORKFLOW_VALIDATION_REPORT.md`, `docs/RBAC_VERIFICATION_REPORT.md`, and `docs/MULTI_TENANT_SECURITY_REPORT.md` for the full evidence behind each "Status (Phase F end)" cell.*
